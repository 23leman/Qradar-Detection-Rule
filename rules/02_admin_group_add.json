{
  "name": "User Added to Admin Group",
  "note": "Detects user added to administrator group - T1078",
  "type": "EVENT",
  "enabled": true,
  "responses": [{"type": "OFFENSE", "descriptor": "User Added to Admin Group"}],
  "groups": ["Authentication"],
  "aql_filter": "SELECT * FROM events WHERE QIDNAME(qid) ILIKE '%member added%group%' OR EventID = '4732'",
  "tests": [{"uid": 1, "name": "EventID", "operator": "EQUALS", "value": "4732"}]
}
