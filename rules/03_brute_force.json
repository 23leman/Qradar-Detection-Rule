{
  "name": "Brute Force Login Attempt",
  "note": "Detects multiple failed login attempts - T1110",
  "type": "EVENT",
  "enabled": true,
  "responses": [{"type": "OFFENSE", "descriptor": "Brute Force Login Attempt"}],
  "groups": ["Authentication"],
  "aql_filter": "SELECT * FROM events WHERE QIDNAME(qid) ILIKE '%failed logon%' OR EventID = '4625'",
  "tests": [{"uid": 1, "name": "EventID", "operator": "EQUALS", "value": "4625"}]
}
